{% include 'default/_form.html.twig' with {
    page_title: 'title_question_delete',
    page_title_param: {'%id%': question.id},
    attr: {
        method: 'DELETE',
        action: url('question_delete', {'id': question.id}),
    },
    form: form,
    submit_label: 'action_delete'|trans,
    back_to_list_path: 'question_index',
} %}